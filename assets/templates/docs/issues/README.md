# Issue Hub

Track real implementation work here.

## Rules

- Create or update an issue doc before non-trivial implementation.
- Use `open`, `in_progress`, or `done`.
- Record the problem, scope, affected paths, and validation.
- Keep `current_backlog.md` updated.
- After completion, also update `docs/status/implemented_features.md` when the feature is real.
